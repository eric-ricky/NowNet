const config = {
  providers: [
    {
      domain: "https://fair-jawfish-26.clerk.accounts.dev",
      // domain: "https://clerk.nownet.top",
      applicationID: "convex",
    },
  ],
};

export default config;
